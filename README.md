# JSW

Demo site for a Jenny Sabo-inspired wellness/journal layout.

## Local dev

```bash
npm install
npm run dev
```

## Deploy

This repo deploys automatically to **GitHub Pages** on push to `main`.

The app is configured with `base: '/JSW/'` in `vite.config.js`.
